# Screenshots

Capture these screens after starting the stack with `docker compose up --build`:

- Login and registration tabs at `http://localhost:8501`.
- Clinical Chat with at least one uploaded report, streamed answer, and citations.
- Document workspace showing parsed files and chunk counts.
- Chat sidebar showing search, rename, pin, export, and delete controls.
- Admin dashboard showing user, file, token, query trend, disease, and medicine analytics.
- Swagger API documentation at `http://localhost:8000/docs`.

Store images in `docs/screenshots/` for portfolio or resume presentation.
